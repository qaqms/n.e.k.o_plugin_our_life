# Changelog

本插件采用"轮次叙事"记录：每一轮写清**病因 / 做法 / 验证 / 测试数**，而不是只列增删。
历史条目只追加、不改写。

## [0.7.0] - 2026-09-15（第十六轮 · 金币系统完善：签到日历 / 猫娘打工 / 玩家打工 / 商店深化）

### 本轮目标（用户提出）

日历 + 每日签到（含连续奖励与幸运事）、打工系统（她拿时间换钱、玩家拿小游戏换钱，
每日限次）、商店深化（稀有度/解锁/每日折扣）。四项全部完工。

### #1 签到日历（commit `89f2b24`）

- **做法**：手动签到按钮 + 月历自绘（UI kit 无 Calendar，与 24 格作息条同一纪律）；
  基础 8 金币、每连续一天 +2、**加成封顶 10 天**；15% 幸运事 +50%~200%（服务端掷骰）；
  漏签可花 12 金币补签（7 天窗、每 ISO 周 1 次，**只续链不给钱**）。
- **关键决定**：连续天数从**日历集合现算**（`core/checkin.current_streak`），不存增量计数器——
  补签天然接链，没有"补签后重算计数器"的特判空间（行为门 `test_makeup_reconnects_streak_without_special_case`）。
- 新面：`checkin` / `makeup` 两入口、`checkin_days` 台账（schema 4→5）、面板新 Tab。

### #2 猫娘打工：真实班次（commit `ffbe722`）

- **做法**：三份工固定在 `core/jobs.py`（便利店 2h/25、吉祥物 3h/40、夜市 4h/60）；
  班次写入分片、tick 按 `now >= job_end_at` 到点结算——重启/关机不丢班，无常驻计时器；
  工钱按下班时刻的精力/心情/健康打 0.8~1.1 折（饿着硬撑挣得少，推回口粮消费）；
  睡眠窗全段冲突不可排班；每日 ≤2 班（代码天花板 3）；早退按已完成时长×折价；
  下班叙事走独立注入通道（`TRIGGER_JOB`，永远 read，与阶段事件同层同理由）。
- **纪律**：表列损耗是**额外磨损**，自然衰减照常（上班不暂停变饿，否则"上班比躺着养精神"）；
  系数不看饱食/好感（饱食再打折 = 双重惩罚；好感不进时薪）。坏班次（鬼 job id）必须自愈清场。

### #3 玩家打工：服务器权威小游戏（commit `c106caa`）

- **病因（设计层）**：面板 `api.call` 的参数是用户可任意伪造的，**客户端上报分数永远不可信**。
- **做法**：心算冲刺（后端出题存答，前端只拿题面，交卷后端判分，限时+5s 宽限）与
  猜大小（牌序**签发时一次抽定**存进 challenge，逐轮只翻自己存的下一张，同点按输）。
  公开视图由 `challenge_public_view` 切分，真值从不出后端（快照泄露面有行为门钉住）。
- **闸门**：一次性（consumed 后重放撞 `game_no_challenge`）；过期作废但**不发钱的局不占额度**；
  每日每种 3 局 + 合计 6 局，计数在后端 `game_counts`。

### 阶段性验证（#1-#3 完工时）

- 全库 454 测全绿（+72：test_checkin 24 / test_jobs 26 / test_games 22）；ruff 干净；
  五门全绿。面板新增三张卡：签到日历（月历+补签）、打工板（目录+倒计时）、小游戏（题卡+押注）。

### #4 商店深化：稀有度 / 隐藏上新 / 今日特惠 / 收藏件（本轮收尾）

- **病灶（手感层）**：六件商品永远在售，收入侧三条通道（签到/班次/小游戏）越修越顺，
  消费侧却没有"值得回来解锁"的终点；而稀有度若只是改标签，不配获取门槛就是自欺。
- **做法**：
  - **稀有度是呈现层**：common/uncommon/rare 三档只画徽章，解锁与折扣判定都不读它
     （否则"把某件改成 common"会静默改变折扣池语义）。
  - **新四件**（`core/economy.py` 物品表）：皇家肉干 18/布丁 15 是消耗品口粮；
    限定礼盒 60/幸运符 88 是**收藏件**——买断后每天产出心情+2/金币+3，
    不可被 `feed` 消耗（`item_keepsake` 硬门），`carry_max=1` 天然限购。
  - **解锁前彻底隐藏，解锁后永不回退**：判据全读已有后端计数器（累计班次≥10 /
    最长连签≥7 / 好感达「亲近」档 / 当日幸运签），唯一新状态是**锁存账 `shop_unlocked`**
    （schema 5→6，旧分片缺键=空账无迁移；写前重走 `normalize_unlocks` 消毒，
    幸运符从机制上进不了账——"当日限定、过期下架"是行为契约）。
  - **今日特惠**：`(日期, 角色卡名)` 的 SHA-256 确定性哈希从可见货里抽 1~2 件 -30%
    （用 hashlib 不用内置 `hash()`：后者按进程 salt，重启一次价格就变）。
    **成交单价在 `shop_entry` 后端复算**——面板参数可伪造，与小游戏同一威胁模型；
    日消费限额按**实付**计。未上架的货只能撞 `shop_locked`；旧版未登记的 `unknown_item`
    顺手收编成已登记的 `invalid_item`（商店/照料同码，旧码在面板上翻不出文案）。
  - **收藏件产出按缺席天数补发、封顶 7 天**（用户拍板；与零花钱"跨天只发当天一份"
    刻意不同——"不上线也在攒"是它的卖点，封顶防长假归来凭空多一笔）；
    买入当天不产，从次日日界线起算；金币直接入账，轴数值走 `apply_item` 夹取路径。
- **验证**：新增 24 门（test_shop.py 23：边界判据/回退锁存/当日窗/哈希退化成常数必红/
  实付价四账同源/补发封顶/旧分片空账；i18n 结构门 +1：新品名与稀有度两语逐项钉住）；
  全库 478 测全绿，五门（pytest / ruff / check / release / hosted-tsx）全绿。
- 货架新字段（`rarity`/`price`/`deal`/`keepsake`/`daily`）全部来自后端快照；
  未解锁的货**不进快照**——隐藏不是前端遮罩，是数据根本没送到浏览器。

## [0.6.0] - 2026-09-15（第十五轮 · 面板细化：把"空空的"填成三层，把 918 行拆成七家）

### 病灶：单文件焊死 ≠ 信息密度（用户反馈："面板给我的感觉就是空空的"）

总览页只有两张卡：一张"没历史就整卡空白"的走势表和一张 9 行文字的 KeyValue 墙；
背包是`肉干: 3`式文字行；事件台账拿着 value/width 却只显示"时间/事件/轴"三列日志味；
而 hours 直方图、anniversary、daily_spent 这些已经送到 context 里的数据根本没露出。
区块全焊在 918 行的 `panel.tsx` 里——想细化一个区块，先要敢改它。

### 做法（呈现拆文件，判据留 Python）

1. **总览页三层化**：今日事实带（第几天/连续/餐数/花销/开口，五个 StatCard）+
   **五轴卡**（每轴：数值 + 档位徽章 + 字符走势柱 + 距下一档差 N 分 + 今日变化）+
   **24 格作息条**（互动小时柱、睡眠窗底色、此刻描边；跨零点在呈现层处理，
   因为"睡没睡"的真判据在 `core/rhythm`，面板不参与裁决）。旧走势卡/节律卡是**被吸收**
   不是被删除：sparkline 进卡、activeHours 进柱、孤零零的 KeyValue 降为四行补充。
2. **背包物品卡 + 一步直达**：只有"在她手上"的物品成卡（数量徽章 + 效果 + 「给她」），
   砍掉"下拉选中→再按照顾"的两步流；商店的批量购买表单保留（囤货是真需求）。
3. **经历时间线**：每条事件挂上"健康 43（跨线 +3）"——value/width 从调试数据变成叙事。
4. **`axes` 新 context 字段**（本轮唯一 Python 数据面）：`core.model.axis_details` 从
   `TIER_BOUNDS` 现算"距下一档"，`_axis_view` 从 inject_history 里挑**今日最早**快照做
   "今日变化"锚点。两条纪律：判据线==档位线（与 v0.4.0 事件钉档位表同源）；
   缺锚点返回 `None` 不假扮 0（面板整行不渲染——拿 0 冒充"今天没变"是编数据）。
5. **文件拆分**：`ui/shared.tsx`（类型 + 纯函数，全仓唯一一份）+ `ui/components/**`
   五张卡；骨架只留状态带、动作通道与 run/refreshContext 单飞闸门。门面调用全仓仍只一条：
   组件不碰调用门面，动作一律回调上提（`Bag.onGive`）。

### 顺手拔掉的真定时炸弹

`test_tick_emits_a_judgment_injection` 在**未改动的 HEAD** 上过夜就红（00:10 实踩）：
默认睡眠窗把非危机注入整层抑制，判断链路的端到端门到半夜变成假红——而假红的门
等于没有门。修法：判断族测试配置显式关 `quiet_during_sleep`（睡眠抑制另由
`test_injection.py` 专测，不靠半夜的巧合顺带验）。

### 验证

`368 → 381 passed`：新增 `tests/test_axes.py` 10 条（判据线==档位线探针、最高档无下一档、
脏值 clamp 不炸、锚点语义含"凌晨跑测试"防抖设（锁 now 同刻，不靠固定前推小时数）、
脏台账跳过不连坐、dashboard `axes.tier == state.tiers` 同源门、无分片时 axes 缺席而面板容错）；
`test_panel_props.py` 扫描面扩到整个 `ui/**`（hosted-tsx 是顺依赖发现的**文件级**扫描，
门只看主文件等于给拆分留盲区）+ 新增拆分结构门、总览三层门、"TSX 不许硬编码档位线"门。
i18n 新键 19 个 × 双语，纯插入 diff（0 删除、CRLF 保持）。

### 刻意不做

轴卡点击详情 Modal（下一轮候选）、多语言剩余六语、任何新入口/新配置键（三处同源面本轮零变动）。


## [0.5.0] - 2026-09-14（第十四轮 · 工具注册心跳：她的判断通道不再静默缺席）

### 病灶：一次启动竞态，她整场失忆（且无人知晓）

三个 `@llm_tool`（feel / company / judge）只在插件启动时经宿主 IPC 注册进
`main_server` 的内存注册表，**fire-and-forget**：那一刻 main_server 没就绪
（"宿主起得比插件晚"）则宿主侧只 warning 不重试（`llm_tool_registry.py` 的 docstring
自己写着 "The plugin can re-register later"——把债写给插件侧）；main_server 重启则注册表
整体清空。两种时序下她都**静默失去**判断工具：数值、吃饭、注入全部照常，
只有 v0.3.0 反馈闭环整条通道无声消失。`forever_companion` 的 host_coord（300s）
已证明这条是承重结构而非可选优化。

### 做法（官方钦定的 resilience 模式：docs/zh-CN/plugins/tool-calling.md）

1. **`services/tool_watch.py`（新）**：挂在 `on_tick` 上的低频巡检器。每 300s 回环
   `GET /api/tools`（stdlib urllib + `to_thread`，4s 超时，零第三方依赖），比对
   公开面 `list_llm_tools()` 的声明与实际在位集合，缺席者逐个重发注册。
2. **三条纪律**：不可达**不盲挂**（只等下轮，不追打）；补挂走 `_notify_llm_tool_registered`
   重发（replace 幂等），不重调公开 `register_llm_tool`（撞 EntryConflict）也不走
   unregister→register（有本地已删、远端未挂的窗口）；认不出的形状按"全场无工具"
   处理——宁可幂等重发，不可漏挂。
3. **永不连坐**：巡检异常全部内部消化，`on_tick` 再套一层双保险；首拍即查；
   与总开关无关（注册韧性是宿主层面在场性，不随业务冻结）。

### 验证

`356 → 368 passed`（`tests/test_tool_watch.py` 新增 12 条：形状差集 / 自节流 /
no_tools 不推钟 / 不可达零补挂 / 点名补缺席 / 单名失败不连坐 / protected 面缺席降级 /
fetch 异常吞掉）；五门发版校验链全绿。README「不联网」声明相应精确化为
「无外网 + 一发宿主回环只读在位性查询」。

## [0.4.5] - 2026-09-14（第十三轮 · 禁用按钮的光标：不是"处理中"，是"按不了"）

### 病灶：总开关关了，按钮却在"转圈圈"（真机反馈）

插件关闭时商店/喂食/纠偏等按钮禁用，但鼠标一放上去就变转圈的"等待"光标——
读宿主 `ui-kit/styles.css:123` 确认：`.neko-button:disabled { opacity: 0.55; cursor: wait; }`。
kit 把"禁用"画成了"忙"，而我们的禁用只是"总开关没开、按不了"。
（runtime.js 的 Button 没有任何内部 spinner，转圈的只是光标本身。）

### 做法

平台层 CSS 只读，按插件侧惯例（`forever_companion` 的 `<style>` 注入已验证可过
hosted-tsx）在面板根部注入一条覆盖：`.neko-button:disabled { cursor: not-allowed; }`。
同特异度后置获胜——kit 样式表必然先于面板内联样式入文档，不用 !important。
新增面板门钉死这条覆盖（`<style>` 注入必须在、not-allowed 字面量必须在）。

### 验证

`355 → 356 passed`；五门发版校验链全绿。

## [0.4.4] - 2026-09-14（第十二轮 · 动作后即时刷新：金币/背包"后台扣了前台不动"）

### 病灶：refresh_context=True 是个没人兑现的口头承诺（真机反馈）

买完东西金币与背包只变了后台、前台不动。读宿主 `ui-kit/runtime.js` 后确认：
`refresh_context` **只有 kit 的 `ActionButton`/`ActionForm` 会自动消费**（runtime.js 两处
`if (action.refresh_context !== false) await api.refresh()`）。而本面板的购买/喂食/
纠偏/开关/重置全部走普通 `Button` + `props.api.call`——动作成功后没有任何人重拉
context。Python 侧八个动作声明的 `refresh_context=True` 一直是空头支票；
上一轮的 10s 轮询只是把症状遮成"最多晚 10 秒"，不是修。
全仓唯一写对的是 `switchFocus`（手动补了一次 `props.api.refresh()`）。

### 做法

1. **单飞 + 尾随合并的 `refreshContext()`**：同一时刻最多一个在途请求；
   在途期间新来的合并为一次"尾随补拉"（`refreshRerun` ref），不排队、不叠加——
   既保证点完购买立刻重拉，又保证慢机器上轮询与动作刷新不会互相堆雪球。
2. **`run()` 成功路径统一调 `refreshContext()`**：Err 以异常走 catch、不动 context；
   八个动作处理器一行都不用改（它们全部经过 run）。
3. **轮询与补拉改用同一通道**：v0.4.3 的"防重入"升级为"单飞合并"，
   后台暂停 / 回可见补拉不变。
4. **`switchFocus` 的手动 refresh 移除**（run("focus") 已走通道），全文件
   `props.api.refresh()` 只剩 `refreshContext` 内一处——由新门钉死，
   谁再手写第二处就是绕过合并闸门。
5. **纯面板轮**：Python 侧零改动（八个动作的 `refresh_context=True` 声明保留：
   它对 ActionButton 形态的调用方仍然有效，也是宿主升级后的自愈面）。

### 验证

`354 → 355 passed`（刷新门升级 + 新增"通道唯一性"门）；五门发版校验链全绿。

## [0.4.3] - 2026-09-14（第十一轮 · 面板自动刷新：手动「刷新」按钮退役）

### 病灶：用户在替插件当定时器

面板数值是后端 tick 结算后、读取时刻按真实时间折算的投影——它每 30 秒就在变，
而面板只在手动刷新时重拉。于是用户学会了一件事：进来先按一下「刷新」。
这是把宿主侧的节律责任转嫁给用户：她的饱食掉了 2 点，要用户按键才知道。

### 做法

1. **自动轮询**：`useEffect` + `setInterval` 每 10s 调 `props.api.refresh()`（与
   `tick_seconds=30` 的结算周期同量级，面板最多滞后 10 秒）。
2. **三条性能闸门**：
   - **防重入**：`refreshBusy` ref——上一轮未返回就跳过本拍，不排队、不叠加请求
     （慢机器 / 插件子进程忙时轮询自动降频，而不是堆雪崩）；
   - **后台暂停**：`document.hidden` 时不拉数据——面板开着没人看就不烧钱；
   - **回可见补拉**：`visibilitychange` 切回前台立刻补一轮，再恢复常态节奏。
   轮询失败静默忽略、下拍重试（toast 在此只是噪音）。
3. **移除两处手动刷新按钮**（状态带 + 空态开关卡）与随之失去使用方的
   `actionOf` / `HostedAction`；Python 侧 `status` 入口**原样保留**（它还是
   Agent / 命令面板的「查看我们的生活状态」，note 同步门的 `panel.msg.statsLoaded`
   键也因此保留）。动作后的即时刷新走 `refresh_context=True`，本就是宿主负责。
4. **纯面板轮**：Python 侧零改动。新增 v0.4.3 刷新门（轮询三闸门各有其形 +
   `<ActionButton` / `actionOf` 不得回潮）。

### 验证

`353 → 354 passed`；五门发版校验链全绿。轮询写法沿用同生态 `forever_companion`
真机验证过的 `setInterval` + `props.api.refresh()` 形态。

## [0.4.2] - 2026-09-14（第十轮 · 真机首跑适配：焦点、 deadlock、流式布局）

### 病灶：真机（Steam 宿主）第一次真跑，把纸面设计照出了三个洞

v0.4.1 的包导入真机后：新装机没有分片时面板只会说"还认不出是哪个角色卡"，
而**总开关藏在只有分片才渲染的状态带里**——用户没有任何办法打开这个插件；
多角色卡时 `@ui.context` 拿不到任何角色信息（宿主调 provider 是零参数的，
`plugin/core/host.py` 源码核实），两张卡以上面板直接退回错误态；
窄面板下 `Grid cols=5` 只有"5 列 / 1 列"两档，五轴挤成一列长条。

### 做法

1. **面板焦点（`focus` 入口 + 独立 store 键 `ourlife.focus`）**：
   归属解析链升级为 `_ctx（本次注入，权威）> 面板焦点（仍是真分片才有效）> 全局唯一分片`。
   焦点指向鬼分片时当场自清（startup 与每次判定时都验）；store 写不进只降级为
   "本进程内可用"——它是体验项不是数据项。焦点键刻意**不**以 `ourlife@` 开头：
   否则分片前缀扫描会把 "focus" 当成一张角色卡。
2. **切换器**：`shards>1` 时状态带出现 `SegmentedControl`（"自动 / 卡名…"，
   "auto" 段发空串清除焦点退回自动判定）；单卡不占地方。
3. **无分片不再递不出开关**：空态分支渲染"开关卡"（Switch + 刷新 + 引导文案），
   新装机第一次打开就能启用。
4. **流式布局**：五轴带与商店目录改 `Columns`（fluid `minWidth`，任意宽度平滑换行）；
   纠偏 `NumberInput` 改 `Slider showValue`；口粮顾问顶部立三个 `StatCard`
   （金币 / 还能撑几天 / 今天吃了）——"还够不够吃"不该藏在 KeyValue 第七行。
5. **去重百分号**：`Progress` label 不再手带数值（kit 右侧自带 `X%`），并取整传入。
6. **面板门扩展**：`Slider`/`SegmentedControl` 的静默坑进 `FORBIDDEN_PROPS`；
   布局门补断言（`<Columns`/`<Slider`/`<SegmentedControl` 与 `run("focus"` 必须在）；
   options 一律 JSX 外构造（文本级门会误伤标签内联对象字面量的 `label:`——本轮被门抓到过一次）。

### 验证

`346 → 353 passed`（`focus` 测试族 7 条：歧义打破 / 鬼卡拒 / 清除退回守卫 / 重启存活 /
startup 自清鬼焦点 / dashboard 跟随 / 持久化故障降级内存）；五门发版校验链全绿。
组件能力面按 **Steam 安装包内的 kit 导出清单**（9-11 build，`PluginDetail-*.js` 内嵌 runtime）
逐项核过：`Columns / Split / Slider / SegmentedControl / StatCard` 全部在场——
本轮用件不依赖比真机更新的宿主。

## [0.4.1] - 2026-09-14（第九轮 · 面板分区：从一滑到底到状态带 + 标签页）

### 病灶：面板是一串 11 张卡的单列长滚

v0.4.0 的功能块已经不少（状态 / 走势 / 顾问 / 商店 / 背包 / 节律 / 感受 / 经历 / 纠偏 / 注入史 / 配置），
全塞在一条道里往下滑：想知道背包还得先跳过走势，想改个数值要滑到底；
而真正需要常驻的信息（她此刻的五项状态）反而滚一眼就丢。

### 做法

1. **顶部常驻状态带**：五轴 Progress + 今日事实徽章（第几天 / 连续天数 / 时段 /
   睡觉 / 距边界分钟 / 需要照顾的轴）+ 总开关与刷新。告警（冻结 / 错误码 / 口粮告急）
   仍在带上方——滚到哪个角落都一眼看到她的样子。
2. **Tabs 四页**：总览（走势 + 相处节律）/ 过日子（口粮顾问 + 商店×背包双栏）/
   她的世界（她自己的感受 + 她经历过什么）/ 管理（纠偏 + 近期注入 + 当前配置）。
   激活页由 kit 的 `useLocalState("tabs:our_life.main")` 持久化，上下文刷新不丢位置。
3. **独立「总开关」卡并入状态带**，不再占一节；其余卡的内容与调用链**逐字不改**。
4. **纯布局轮**：Python 侧零改动——context / action / 码 / 台账 / 注入全部原样；
   只加 `panel.band.*` / `panel.tab.*` 文案键（双语纯插入保 CRLF）。
5. **新增一条文本级布局门**（`test_panel_uses_tabbed_layout_with_a_persistent_status_band`）：
   Tabs/StatusBadge 必须在、四个页签键与状态带键必须在、Tabs 持久化 id 不许改——
   防止后来的改动无意把面板打回单列长滚。

### 验证

- `pytest tests -q` → **346 passed**（新增 1 条布局门）；
- `tools/release_gate.py` 五门全绿（pytest / ruff / check / release / hosted-tsx）。

## [0.4.0] - 2026-09-14（第八轮 · 阶段性事件与面板叙事）

### 病灶：数值只会"连续地"动，她身上发生的事没有名字

v0.3.0 的数值已经算得很细——五项轴、作息感知、经济循环、连她自己的判断都回流了。
但**读数与经历是两回事**，而当时只有读数：

- 她病了三天又好了，面板上只看得见"健康：生病 → 一般"，看不见"她病过、然后好了"；
- 她闹过脾气又被哄好了，同上——`tier_change` 如实播报档位变化，但那是**仪表盘**的口吻；
- 更糟的是**说得出的那句也不对**：跨档注入讲"身体从「生病」变成了「一般」"，
  这是把面板上的标签念给她听。她该说的是"你身上那场不舒服终于过去了"。

于是"养成"少了最容易上瘾的一环：**转折被记住**。这一轮把那种时刻从连续读数里摘出来，
给它一个稳定的名字，并让它在两个地方出现：她嘴里（注入叙事）、面板上（她经历过什么）。

### 做法

1. **`core/events.py`（新，纯函数）**：两个事件 + 四个设计决定。事件名是稳定 ASCII
   （`sick_recovery` / `cheered_up`），面向用户的文案走 `panel.event.<键>`。
2. **判据挂在档位线上**：病愈 = 健康跨过 40（脱离 sick+frail 两档，因为那两档面板都读作"病着"）；
   哄好 = 心情跨过 20（脱离 sulking；`low` 已是"低落"，能哄了，不算事件）。
   两条线**直接引用 `TIER_BOUNDS`**，模块 import 时自检——面板显示她脱离了生病档，
   与插件认为她病好了，永远是同一个判据。刻意**不用**可配置的 `crisis_*` 阈值：
   那是注入旋钮，用户调它不该改变"什么算病好了"。
3. **同轴抑制**：她还在这个轴的危机档里就不发——"你终于好起来了"与"你现在病着"
   摆在同一段上下文里是自相矛盾的。判据只取同一条轴。
4. **独立注入通道**（`services/injector.plan_for_event`）：**刻意不插进 `plan_for_tick` 的优先级链**。
   塞进去会两头错——"刚病好但还饿着"时病愈被饥危机挤掉，反过来好事也会把饥危机顶掉。
   两条各走各的，只共享 `max_per_hour` 总闸门（不开第三条频控）。
5. **睡眠分家**：`pick_event` 不认识"睡觉"这件事，**永远**返回事件；睡眠抑制在注入层
   （`wake_suppressed`）。为什么必须分：睡过去的事件也要记台账（面板上她该有这段经历），
   而只要记账就必须无条件——否则下一拍会把它重新识别一遍，冷却靠的正是这本台账。
   分完的结果是：**病愈（危机解除）允许在睡觉时也开口，哄好不吵醒她**。
6. **冷却按事件走，不按轴**：哄好 6 小时（心情是小时级，掉下去又上来是常态）、
   病愈 20 小时（比一天略短，允许"隔天又病了、又好了"这种真的两次经历）。
   台账 12 条上限 > 最紧冷却窗口，所以老条目被挤掉不会让冷却失效。
7. **台账只存事件名 / 轴 / 时刻 / 数值快照**（schema 3 → 4）：与判断台账同一条隐私纪律，
   不进任何对话正文。旧分片不需要迁移脚本（缺键即空台账）。
8. **面板新增「她经历过什么」块**：经历列表（时刻 / 事件 / 轴）+ 一段说明，
   顶掉"为什么这一列比注入记录长"的疑问（睡过去的事件也记）。

### 刻意的边界（都不是疏漏）

- ❌ **不给事件配数值奖励**：纪念日已经会给礼物，再配一遍会让"康复"变成刷数值的路径。
  事件**只叙事**——这也让它保持纯函数、可确定性单测。
- ❌ **生日留到下一轮**：它需要"角色的生日"这个新配置面与用户真实输入，与代餐成长、
  情绪感知同属需要单独设计的项。本轮不动既有的纪念日（相处 N 天）触发器。
- ❌ **情绪感知**（读对话正文判心情）仍是既定边界，需确认后以独立插件联动方式接入。

### 数据与兼容

`schema 3 → 4`：新增 `event_history`（有界 12 条）。旧分片（v1/v2/v3）读进来即空台账，
**不需要迁移脚本**——沿用 `ShardState.from_payload` 的逐字段回退契约。
新增配置段 `[our_life.events]`（`enabled` / 两个冷却），`plugin.toml` 与 `config.example.toml`
逐键逐值同源（同源门守着），且**没有阈值旋钮**（两条线是模型常量）。

### 验证

`308 → 345 passed`（新增 37 条门，`tests/test_events.py`）。覆盖四类不变量：

1. **判据线 == 档位线**：反查 `tier_index_of` 断言"线下一点点仍读作病着/闹脾气"，
   而不是断言"线 == 40"（后者会在档位表整体平移时假红）。
2. **只认向上跨过**：闭区间边界（停在 40.0 上算好了）、向下掉、停在线上、
   以及"从低落升到平静"刻意不算事件的四类反例。
3. **冷却**：窗口内外两侧、按事件不按轴（病愈冷却不压哄好）、0 冷却 = 永远允许、
   以及**脏台账**（`None` / 字符串 / 缺键 / NaN / 未来时刻）不许炸也不许永久抑制。
4. **睡眠分家**：参数化六种 `(事件, 睡/醒, 静默开关)` 组合；
   外加一条**防回归门**——断言 `pick_event` 的签名里没有 `sleeping`，
   谁把睡眠判断挪进 core 层就会红。

另加台账形状门（JSON 安全、键集恰为 5 个、两位小数）、`schema 3 → 4` 兼容门
（旧分片仍能读、旧判断台账不丢）、面板快照不带正文、以及"同拍两件事取更重的"。
`test_config_docs_sync.py` 的 `SUB_SECTIONS` 加了 `events`（自动获得键集/默认值同源门）；
`test_i18n_contract.py` 加了事件族的**结构门**（拼键不在引用面门覆盖面内，少一个键只会在
面板上显示空白——所以逐项钉住）。

`ruff`（CI 逐字参数）0 error；宿主 `neko-plugin check` 0 error（唯一 warning 是
"工作树有未提交改动"，提交后消失）；**五门全链通过**——本轮本机 `release` 与
`hosted-tsx` 两门**首次跑通**（上一轮记的 `PermissionError` 在这个工作区不复现，
因为插件源在仓库外、由 `release_gate` 复制进宿主探针目录）。

## [0.3.0] - 2026-09-14（第七轮 · 反馈闭环：她自己的判断回流成数值）

### 病灶：成长只按"发言条数"算，失真在两个方向同时发生

v0.2.0 的数值能算得很准，但成长只有一个入口：`apply_turn_gain`，按用户发言的**条数**给。
于是——

- 用户连发 20 条「嗯」「哦」「好」→ 和认真聊 20 句拿到**一模一样的**步长；
- 反过来，用户认真陪她聊了一小时但只发了 3 条长的 → 她"感觉"只有 3 条的量。

**模型（她本人）是唯一能判断"这一轮到底聊得怎么样"的一方**。这一轮给她一条回传通道。

### 做法：不是"数值写入接口"，而是有上限的修正项

设计总纲写在 `core/judgment.py` 的模块 docstring 里（改这个模块之前必须先读）：

> 她的判断**不是数值写入接口**，而是一个有上限、可衰减、需要真实互动作前提的修正项。

四个标签 + 一个默认：`wonderful` / `good` / `neutral` / `dull` / `hurt`。
她通过 `our_life_judge` 工具回传，**四道硬闸门**把它压在一个很小的幅度里：

| 闸门 | 机制 | 防的是什么 |
|---|---|---|
| ① 单次幅度 | 修正分量 = `GrowthSettings.turn_*_gain` × `_TURN_GAIN_SCALE`(0.6) —— **常量引用，不写死数字** | 工具被滥用也压不过真实互动；调了 `turn_mood_gain` 这条不变式自动跟着成立 |
| ② 会话递减 | 同会话内第 n 次按 `1/(1 + n*session_damp)` 递减（复用既有的 `session_turns`，跨会话归零所以自动回到满步长） | 一轮里反复回传刷额度 |
| ③ 每日上限 | 正向 / 负向**两本独立的账**（默认 8.0 / 4.0 心情分），用尽即拒 | 单次幅度再小，没有日上限也能被磨到满——这是唯一真正的止损线 |
| ④ 真实互动作前提 | `last_touch_at > last_judgment_at` | **模型凭空给自己加分**（在没有任何用户发言的回合里反复调用工具） |

**为什么负向也要闸门**：让工具能扣分等于给模型一条**惩罚通道**，所以负向走独立且更小的
日预算（只有正向的一半），且同样需要真实互动作前提——它只能是"这轮聊得敷衍"的轻微表达，
不可能把一个角色卡刷成闹脾气。

模型输出一律当**不可信输入**（台账交接要点第一条）：标签只认白名单，非法/缺失/大小写混写/
夹空格的输入**收敛成 `neutral`**（权重 0，无害空操作，**不报错也不改数值**——报错只会把噪声
引回对话）；强度夹到 `(0,1]`；工具**永不抛异常**；**返回值绝不带数值**
（否则模型会开始念叨"我心情 62 分"——与注入契约"不给模型看数字"是同一条纪律）。

### 刻意的边界（都不是疏漏）

- **不动 `apply_turn_gain`**：判断是**叠加的修正项**，不是替代品。所以 v0.2.0 的 263 条门
  一条都没改，回归面为零。
- **不做情绪感知**：插件刻意不读总线记录的 `content`（有隐私门守着），这条边界不能因为
  加了个"感受"通道就松动。她的判断只能来自她自己。
- **不加随机性**、**不让工具设绝对值**（只能给枚举标签，数值由 `judge` 算）。
- **反馈不碰饱食与精力**：那两轴由作息与进食驱动，让判断去改会和 `apply_meal` 打架。

### 接线：工具只记账排队，数值由 tick 施加

工具在调用那一刻完成四道闸门与预算记账，把**已经批准的增量**排进进程内队列
（刻意不持久化：重启后丢掉一条未消费的判断是正确行为）；tick 在**折算之后**施加它——
顺序很重要，否则长时间没跑 tick 时修正量会被随后的整段惰性衰减顺手吃掉。

⚠️ **实现期被自己的门抓到的 bug**：第一版排队的是**标签**，tick 拿到后再跑一次 `judge`。
结果一次互动被两条路径各判一次，而第二拍时 `last_touch_at` 没有前进，
于是合法判断被闸门 ④ 判成 `judgment_needs_interaction`，**永远施加不上**。
改成排队"已批准的增量"、tick 只施加不判定。`test_tick_applies_the_pending_judgment` 走
**真实顺序**（真对话 → tick 写入 `last_touch_at` → 判断 → 下一拍施加）钉住这条。

### 顺手收掉的一处旧漏（同源）

`services/injector._drifted_since_last_injection` 只比较了好感/心情/健康三轴，
而 v0.2.0 加了饱食与精力却漏在这里——结果是"**她饿到掉档但心情没动**"这种最该说话的
情形反而不触发漂移注入。改成比较全部五轴（`STAT_NAMES`）。

### 数据与兼容

`ShardState` schema 2 → 3：新增判断台账（`judgment_day` / `judgment_used_add` /
`judgment_used_subtract` / `judgment_count_today` / `last_judgment_at` / `judgment_history`）。
**台账只存标签、时刻与幅度，不存任何对话正文**——工具签名里只有一个枚举 + 一个数字，
就是为了让"模型把对话原文写进持久化状态"这条路根本不存在（有专门的门核对台账键集）。
旧分片（v1 / v2）不需要迁移脚本，`from_payload` 逐字段回退。

### 验证

`263 → 308 passed`（新增 45 条门）。⚠️ **308 是"ruff 在位"时的数**：直连 `.venv` 跑是
`307 passed / 1 skipped`——那条 skip 是发版链自己的环境自检门（ruff 缺席时按设计跳过），
两种数都对，别当成回归。`test_judgment.py`（新，42 条）里**每条闸门都有独立的正/反用例**——
非法输入收敛、单次幅度严格小于一次真实互动、幅度随 `turn_mood_gain` 联动、会话递减的精确比例、
日预算的耗尽/裁剪/方向隔离、闸门 ④ 的"同一次互动只能消费一次"、工具永不抛异常、返回值不含数值、
隐私键集、schema 2→3 兼容、跨天归零；`test_config_docs_sync.py` 新增 2 条把两条不变式钉在
配置面上（单次幅度 < 一次真实互动、负向预算不许超过正向）。

反向对照**逐条做过**（撤掉修复必须变红），七项全部命中：幅度系数调大（0.6 → 5.0）、
撤掉会话递减、预算恒无穷、闸门 ④ 放行、未知标签透传、总开关失效、
以及本节那条接线 bug 的原始写法。
`ruff`（CI 逐字参数）与宿主 `neko-plugin check` 均 0 error。

`release` / `hosted-tsx` 两门仍受沙箱所限本机验不了（要写宿主仓
`<宿主>/plugin/plugins/`，实测 `PermissionError`），以 GitHub Actions 的 verify 工作流为准。

### 本轮**有意没做**的事

- 病愈 / 哄好 / 生日等阶段性叙事（台账建议与反馈闭环同轮才有味道，但本轮要求
  "单独一轮做透"，所以留到下一轮）。面板上的"她自己的感受"块已经把这轮的读数接好了，
  叙事可以直接在它旁边长出来。
- 情绪感知（读对话正文判心情）——仍是既定边界，需确认后以独立插件联动方式接入。
- 其余 6 语言 i18n（本轮只补了中英两份的新键，键集一致门守着）。

## [0.2.0] - 2026-09-14（第六轮 · 「过日子」主线：作息 + 经济 + 五轴）

### 病灶：数值只有"往上推"和"往下掉"，没有"出路"

v0.1.0 的三轴模型能算得很准，但**没有消耗端**：互动只往上推、冷落只往下掉，
于是"养成"退化成"看两条曲线"。真机上跑了几天的结论是——用户没有理由**每天回来一次**：
数值不会因为他不回来而发生任何"需要他管"的事。

这一轮补上的是**生活本身**：她有作息（夜里会睡）、有肚子（会饿）、有口袋（要花钱），
以及一条会持续产生压力的日常循环：**她每天自己吃掉背包里的口粮**。
主人囤得够、按时回来，日子就过得下去；囤的吃完了而没回来，她就会饿。

### 做法（五块，各自可单测）

1. **`core/rhythm.py`（新）**：时段判定、睡眠窗（支持跨零点，`24` = 24:00）、
   相处天数与纪念日（锚点固定 `ANNIVERSARY_DAYS`，与面板"还有 N 天"同源）。
   不猜作息：睡眠窗是配置项，推断错了会直接暴露成"她半夜不困"。
2. **`core/economy.py`（新）**：物品表（`meat/fish/cake/medicine/toy/gift`，枚举单一来源）、
   背包、自动进食选品、购买校验（含携带上限与当日消费上限）、日薪、**口粮顾问**。
3. **`core/model.py`（扩）**：新增 `satiety`（饱食）与 `energy`（精力）两轴；
   `apply_decay` 变成**节律感知**（清醒/睡眠分段积分、深夜熬夜额外耗）；
   新增 `apply_coupling`（饿 → 心情掉得更快；累 → 健康恢复更慢）、`apply_meal`、`apply_item`、
   `crisis_axes`（危机判据扩到四轴）。`STAT_NAMES` 扩到五项，面板顺序 = 精力/饱食/心情/健康/好感。
4. **`services/` + `__init__.py`**：tick 变成"折算 → 采样成长 → 冷落 → 金币与日账 → 进食 → 纪念日 → 注入"；
   新增四个面板入口 `feed`（照顾她）/ `shop`（买东西）/ `allowance`（补贴）/ `advisor`（她每天吃多少）。
   `push_message` 加 `coalesce_key`（同一条理由折叠）与 `respond` 独立小时上限。
5. **`ui/panel.tsx`（重写）**：五条 + 走势（字符画折线，Hosted UI Kit 没有图表组件）+
   口粮顾问 + 小商店 + 背包 + 节律/纪念日；商店卡片带"已有一个/没有"与一键购买。

### 三个设计决定（都写进 docstring，防后人当成疏漏）

- **睡眠期照吃**：只有"饿到阈值"这一道闸，不额外加"必须醒着"。否则默认睡眠窗（8 小时掉 12 分）
  会把饱食压到阈值以下，她一醒来就同时"饿着"且"今天还没吃过"，账面对不上。
- **冷落只扣心情/健康/好感**：饱食与精力由作息与进食驱动，再按天扣一次就是双重惩罚。
- **`respond` 只在真的处于危机档时触发**（v0.1.0 是"危机或跨档"）：跨档进上下文就够了，
  否则她会在一天里反复主动开口，打扰感盖过陪伴感。另外睡觉时压住非危机注入。

### 一个被自己的门抓住的真 bug

第一版**同时**在 `apply_decay` 与 `apply_coupling` 里放了耦合系数，于是低精力那一侧
健康被恢复两次、反而比休息好的一侧恢复得更多——真机语义直接反过来。
`tests/test_model_rhythm_axes.py::test_decay_alone_carries_no_coupling` 把它钉住了：
`apply_decay` 只管时间，耦合只由 `apply_coupling` 施加。

### 数据与兼容

`ShardState` schema 1 → 2（新增两轴、金币、背包、吃饭账、纪念日、相遇日）。
**旧分片不需要迁移脚本**：`from_payload` 逐字段回退默认值，真机上跑着的 v0.1.0 分片
升级后直接读进来（有专门的门 `test_v1_shard_is_read_with_v2_defaults`）。

### 验证

`152 → 263 passed`（新增 109 条门；沙箱里 ruff 不可用时是 `262 passed / 1 skipped`，
那条是发版链门按环境自检）：`test_rhythm.py`（新，35 条）、`test_economy.py`（新，25 条）、
`test_model_rhythm_axes.py`（新，25 条）、`test_entries.py` 从 41 条增到 60 条
（新增 19 条集成门：自动进食 / 没粮会饿 / 不饿不吃 / 日薪与零花钱 / 商店与稳定码 / 携带上限 /
照料 / 顾问 / 睡觉静默 / 饿坏了照旧开口 / v1 兼容 / 纪念日不重复），
`test_config_docs_sync.py` 新增 2 条（口粮种类必须真实存在且能吃、`meal_threshold` 必须落在"吃饱了"档内），
`test_panel_props.py`（新，4 条）补上本机验不了的 `hosted-tsx` 那一类：
面板里写 Kit 没有的 prop 会**静默丢弃、布局默默坏**（本机沙箱跑不了真 `tsc`，
所以用文本级门把已知静默坑钉住；同时复刻检查器的"裸门面对象"与"裸 npm import"判据）。
`ruff`（CI 逐字参数）与宿主 `neko-plugin check` 均 0 error。
`release` / `hosted-tsx` 两门仍受沙箱所限本机验不了（见台账 §4）。

## [0.1.0] - 2026-09-14（第五轮 · `store_unavailable` 死码收口）

### 病灶：一个为"改动没落盘"写好的码，从来没人接线

`core/codes.py` 里登记着 `store_unavailable`，两份 i18n 里也有对应文案
（"存储暂时不可用，这次改动没有落盘" / "Storage is unavailable, so this change was not
persisted"）——但全仓没有一处返回它。于是 store 通道真的不在时，插件照样回
`stats_loaded` / `stat_updated` / `stats_reset`，等于告诉用户"一切正常"，
而实际上数值只在内存里、重启就丢。

**判据（本轮定下的边界）**：只有**"store 通道对象整个缺席"**才算 `store_unavailable`。

- `plugin/sdk/shared/core/router.py` 的 `store` 是
  `getattr(main_plugin, "store", None)`，`None` 是**真实可达状态**，不是防御性摆设。
- `get` / `set` 返回 `Err` 的**瞬时降级不算**：本插件的既定契约是"持久化失败只降级、
  不抛给调用方"（`services/state.py` 模块 docstring），那种失败每拍都可能发生，
  算成"不可用"会让面板横幅一直挂着——属误报。

**做法**：`StateStore.store_available` 作单一来源（替掉 dashboard 里那处就地 `getattr`），
在 `dashboard_context`（挂 `error_code` 让面板显示横幅）、`status_entry`、
`tune_entry`、`reset_entry` 四处如实回码。

**刻意不动的一处**：`switch_entry` 走的是**配置层** `self.config.set`，与数据 store 是两条
独立通道，用 store 缺席与否去挡开关会是误报——所以只留一条 warning 记痕，语义原样。
这个决定有专门的常驻门（`test_switch_still_works_without_the_store_channel`）钉住，
免得后来者"顺手"把它一起改了。

**验证**：`tests/test_entries.py` 新增 5 条门；反向对照做过——把 `store_available` 改成恒
`True`、把横幅码换成别的码，对应门均变红，还原后全绿。148 → 152 passed。

### 同轮修掉的：发版链卫生门自身"靠环境才能跑"

上一轮那两条门在受限沙箱里**只能靠 skip 蒙混**，本机根本验不到。根因是测试自身不隔离：

1. **`test_lint_failure_is_not_misreported_as_a_cache_miss`**：原来真起 `uvx` 子进程做探测，
   而 uv 会在临时目录里留下自己的缓存，回收时撞沙箱权限（实测 pytest 报
   `PermissionError ... tmpduf7bqx7`）。改成**把探测结果钉成 True**（只验"探测命中之后"的
   分支），违规样例改写在**工作区内**并带下划线前缀（pytest 不收）。

   ⚠️ **这次重构我自己踩了一个坑，而且是被反向对照抓回来的**：重写时把 `capsys` 参数丢了，
   而那句"缓存未命中"是 `print` 到 stdout 的、**不在 `_run` 的返回值里**——于是断言恒真、
   门静默失效。反向对照（注入误报）时它仍然绿，才发现。断言现在挂在 `capsys` 上，
   教训也写进了该门的 docstring：**验证"有没有打印某句"必须捕获 stdout，不能只看返回值**。
2. **拆出 `test_offline_probe_is_actually_probed`**：上一条把探测结果钉住了，探测本身
   就没人看着——这条用桩替换 `_run` 验"命中走离线 / 未命中走联网、两种都钉住版本"，
   任何环境都能跑，不依赖网络与缓存。
3. 新门一度用了 `tmp_path` fixture，而 `%TEMP%` 下的 pytest 临时根在本机不可写
   （`PermissionError ... pytest-of-lulu`）——它本来也没用这个 fixture，去掉即可。

**本机实测**（需把 uv 目录指进工作区，见交接台账 §4）：

| 门 | 结果 |
|---|---|
| `pytest` | ✅ `154 passed`（此前 `152 passed / 1 skipped`，两条环境门现在真跑） |
| `ruff` | ✅ `All checks passed!` |
| `check` | ✅ `[OK]` |
| `release` | ❌ 链断在这里：`PermissionError ...\N.E.K.O\plugin\plugins\our_life` |
| `hosted-tsx` | ❌ 同上（探针副本也要落在宿主仓内） |

即 **3 / 5 门本机可验**；后两门是"独立仓本地全绿 ≠ CI 绿"的守门人，
**发版前必须在正常环境补跑**——这是下一轮的第一件事，不是可以省的一步。



## [0.1.0] - 2026-09-14（第四轮 · 接手复核：三个真问题）

接手时的基线是"五门全绿、132 passed"。复核后确认三处真问题——**其中两处让"全绿"这件事本身
不可信**（门比被测物先死），第三处是真机 store 里躺着的实际错误行为。

### 1. 发版链在"打印结果"这一步崩掉（报告先死，门是好的）

**病因**：Windows 的 `sys.stdout.encoding` 取的是**活动代码页**（本机 GBK），
**输出被重定向时也一样**（后台任务 / 管道 / CI 捕获都会走进去）。于是
`print("\n链路中断 ❌")` 抛 `UnicodeEncodeError: 'gbk' codec can't encode character '\u274c'`，
整条链在打印报告时崩掉。真机实测的现场就是这样：`pytest` 门 `[OK]`、`check` 门 `[OK]`，
然后在 `print` 上崩——**看起来像插件有问题，其实是报告自己死了**。
README 的"坑位存档"第 4 条明明写着"子进程管道与 stdout 必须钉 UTF-8"，但当时只钉了管道那一侧。

**修法**：模块 import 时按"是否 tty"分两条路兜住 stdout——重定向（管道 / 捕获）重配
UTF-8 + `errors=replace`；真控制台保持编码不动、只降到 `errors=replace`（否则中文字节在 cp936
控制台会变乱码）。子进程另用 `PYTHONIOENCODING=utf-8` 钉死，下游 gate 的日志不再变成
`����ʱ 0.7s` 这种读不出耗时的乱码。

### 2. ruff 门把整条链拖死在一个网络依赖上

**病因**：门里固定用 `uvx ruff==0.12.4`。`uvx` 每次都会去 PyPI 解析一次——断网 / 代理不通时
它重试三次然后失败，于是 **ruff 门一红，后面三门（check / release / hosted-tsx）根本没机会跑**，
报告却指向"插件有问题"。

**修法**：工具**钉死版本 + 离线优先**。先在 uv 缓存里按同一个钉住版本跑（`--offline`，断网也照跑、
版本与 CI 逐字一致），缓存没命中才联网解析一次。两条路都不通就明确报错并打印预热命令——
**绝不**顺手用 PATH 上那个版本未知的 `ruff` 顶上来（那等于在没人知道的情况下换了门）；
真要用它必须先核对版本一致。

**中途自己踩到并修掉的**：第一版写成"离线跑一次，失败就当缓存未命中、再联网跑一次"——
可 ruff 因**代码里有 lint 错误**而退出码 1 也是"失败"，于是正常的 lint 失败被误报成缓存未命中，
多跑一次联网还打出一句误导人的"缓存未命中"。报告里的假信号比门本身红更坏（会把人带去查网络）。
改成先用 `--version` 探缓存是否命中、只在诊断输出确实指向"离线取不到工具"时才回退联网。
留了常驻门 `test_offline_probe_hit_does_not_misreport_a_lint_failure`（用真 lint 违规逼门红，
断言报告里不出现"缓存未命中"）。

### 3. 分界线上的亚分噪声白送一次强注入（真机 store 里抓到的）

**病因**：分档线是 `value >= lower` 的硬比较，而**默认好评 20.0 正好压在**
stranger/acquainted 的分界线上。只要落一个 30 秒心跳，`apply_decay` 就把它折成
`19.9998456796`，硬比较立刻判"跨档"，于是发一条 `tier_change` 强注入——
面板上数值还是 20.0、档名没变，用户看不到任何变化，注入文案却写着"关系档位刚刚变了"。

真机证据（部署态 `store.db` 的 `ourlife@MS` 分片，修复前）：
`inject_history` 里唯一一条记录就是 `trigger=tier_change`、
`stats.affection=19.999845577439277`；`stats.affection` 已是 `19.9978…`。

**修法**：新增 `crosses_tier_boundary` / `eventful_tier_transitions`（`core/model.py`），
判据带 **0.05 分迟滞**：两点都紧贴它们之间那条分界线（`|v - 边界| < margin`）时，
这次跨越只算噪声；否则算真事件。0.05 分用户读不出来（面板只显示一位小数），
心情走完它约需 24 秒、好感约需数月——语义无损。
**硬比较的 `tier_of` / `tier_transitions` 原样保留**（面板与"数值算哪一档"必须忠实反映），
只有 `__init__.py` 的 tick 注入判定换成带迟滞的版本。

**验证**：新增 17 条门（模型层 7 条 + 集成层 2 条 + 发版链卫生 8 条），并且**逐条做了反向对照**：
撤掉 stdout 兜底 / 去掉 `--offline` / 把 lint 失败误报成缓存未命中 / tick 改回硬比较 /
迟滞余量清零 —— 对应的门全部变红，还原后全绿。`132 → 148 passed`。

### 复核结论（本轮**没有**改的东西，都是有意的）

- 真机部署态未被触碰（`%LOCALAPPDATA%\N.E.K.O\plugins\our_life\` 的 config/data 原样保留）。
- `README` 里"其余 6 语言 i18n / 情绪感知 / 工具重注册心跳 / 趋势图表"四项仍是已知限制，
  按 README「已知限制」与「路线图」处理，本轮不动。
- 真机 store 里那条已经写歪的 `tier_change` 历史记录**不清理**：它只是历史展示，
  下一次注入会自然把 `inject_history` 往前推；擅改用户数据比留着更危险。

## [0.1.0] - 2026-09-14（第三轮 · 真机测试前加固）

### 冷启动空转风险（真机才会暴露的那类）

**病因**：后台心跳是定时器，**没有 `_ctx`**，它决定"结算哪个角色卡"只能靠
"已知分片 ∪ 总线记录里的角色名"。冷装机上两者都可能为空（若宿主的对话总线记录不带
`lanlan_name`），插件会安安静静地空转——面板看着正常，数值永远不动。

**修法**（两条，都不改注入语义）：

1. **碰过就 bootstrap**：面板与入口解析出角色卡后，落一个初始分片（每个角色卡每进程只落一次，
   避免面板刷新写 store）。"用户打开过面板"本身就是"这个角色卡在用"的信号。
2. **空转诊断**：认不出角色且有记录时，打一条 warning，并打印**记录键名**（只打键名、
   不打任何值，避免对话正文进日志）——真机上定位问题时一眼能看到宿主的真实记录形状。

**验证**：新增两条门（bootstrap 落盘、每进程只落一次且不覆盖已有值）；`130 → 132 passed`，
五门全绿。

### 发布产物卫生：`__pycache__` 不入包（上游问题的调用侧绕法）

**病因**：上游打包器的元数据探测会**以子进程 import 插件本体**，而 import 的目标是"已经过滤完的
暂存树"，CPython 于是把 `__pycache__/*.pyc` 写进 payload 并归档。本插件首版产物因此多出
12 个 `.pyc`（36 条目 → 128 KB）。同一现象已在
`dist/upstream-issue-packager-pycache.md` 里被记录（forever_companion v1.2.1 多 21 个、体积 +73%），
属于低severity的上游缺陷。

**修法**：探测子进程**继承环境变量**，所以在调用侧设 `PYTHONDONTWRITEBYTECODE=1` 即可——
`tools/release_gate.py` 的所有子进程统一带上它（这也是那份报告给出的首选修法）。
产物变为 **24 条目 / 0 个 .pyc / 128 KB**。

⚠️ **不能事后删包里的 `.pyc`**：那会让 `metadata.toml` 的 `payload.hash` 与实际内容不符，
宿主导入时的完整性校验会不过。

## [0.1.0] - 2026-09-14（第二轮 · 五门打通 + 双态修正）

### 打通了五门

`pytest` / `ruff` / `check` / `release` / `hosted-tsx` 全绿。其中 **release 门是在真挂载态通过的**：
插件被真的复制进宿主 `<host>/plugin/plugins/our_life/`，再按**裸 id** 跑 `check -r`
（校验 → 插件本地测试 → 打包 → 包内 payload hash 复核），随后无条件清理宿主仓副本。

### 两个真实缺陷（都是"独立仓全绿、挂载态/CI 全红"这一类）

1. **挂载态下测不了**：能导入真 SDK 时，真 `NekoPluginBase` 会自己建一套真通道
   （真 `PluginStore` / `PluginConfig`），`bus` 更是**只读 property**，测试既接管不了、
   一碰就是 `TransportError: ctx.get_own_config is not available`。
   根因有两层：①`make_plugin` 夹具绕过了通道接管逻辑；②即使接管，`bus` 也无法赋值。
   修法：测试进程里**无条件**把 `plugin.sdk.plugin` 换成桩门面，让两种形态行为一致
   （挂载态真正要验的裸 id 目录 / entry 解析 / 清单校验 / 打包，由 `check -r` 自己覆盖）。
2. **hosted-tsx 检查器是文本级规则，连注释都不跳过**：面板里一句解释性注释写了
   "`api.call()` 返回的是信封…"，就被判成"用了全局对象"直接拒收。
   同时确认：从 props 解构出与全局对象同名的短名字也会被拒（catgirl_seiyuu 台账同款坑）。
   修法：注释与代码都避开该标识符的裸形态，一律写完整的成员访问形式。

### 反向对照（确认门不是摆设）

- 删掉一个 `panel.errors.*` 键 → i18n 契约门 **2 failed**；
- 只改 `plugin.toml` 的 `mood_tau_hours`（3.0 → 4.0）→ 文档↔配置同源门 **2 failed**；
- 把 `[plugin.store].enabled` 改成 `false` → 静默失效门 **1 failed**。
还原后 `130 passed`。

### 已修掉的（本轮）

- pytest 的 `<Package <目录名>>` 节点会以模块名 `"__init__"` 导入仓根 `__init__.py`，
  触发无父包相对导入崩溃 → conftest 预注册 `sys.modules["__init__"]` + 目录强制按 `Dir` 收集。
- CI 的 ruff 门带 `--ignore-noqa`：裸 `import plugin.sdk.plugin  # noqa: F401` 会被判成未使用导入，
  且 ruff 的修复动作是**删掉整行**（桩探测就此失效）→ 改用 `importlib.import_module` 探测。
- 两处 `import pytest` 与自家包之间的空行不符合 isort 对第三方包的归组 → 已对齐。

## [0.1.0] - 2026-09-14（第一轮 · 首版）

### 落地的东西

- **数值模型**（`core/model.py`，纯函数零 SDK 依赖）：好感度 / 心情 / 健康三项，惰性衰减
  （`exp(-Δt/τ)` 读数时折算）、心情与健康向静息基线回落、好感只缓慢回落不自动回升、
  分档阈值固定为单一来源（不进配置）。
- **行为统计**（`core/behavior.py`）：只读宿主 `bus.conversations` 快照，按 `lanlan_name`
  归属 + `conversation_id` 去重，聚合出用户发言数 / 最近互动 / 连续相处天数 / 时段分布。
- **强注入**（`core/injection.py` + `services/injector.py`）：事件驱动（危机 / 跨档 / 跨天首触 /
  显著漂移）+ 三重频控（间隔、每小时上限、字符预算）；注入正文不给模型看原始数字与档名。
- **入口与面板**：`status` / `tune` / `reset` / `switch` 四个入口 + Hosted TSX 仪表盘；
  fail-closed 总开关默认关闭。
- **她可自主调用的 LLM 工具**：`our_life_feel`（查状态）、`our_life_company`（索取陪伴，带冷却）。
- **中英双语 i18n** + 常驻契约门（码形 / 码↔键双向同步 / 键集一致 / 引用面 / tier 与 trigger 结构门）。
- **文档 ↔ 配置同源门**：`plugin.toml`、`config.example.toml` 与 `core/configuration.py` 的默认值
  必须逐键相等（防"默认值漂移"这类历史债）。
- **五门发版校验链** `tools/release_gate.py`：pytest / ruff / check / release / hosted-tsx，
  其中 release 门真的挂载到宿主 `plugin/plugins/our_life/` 后按裸 id 跑 `check -r`。

### 开发期抓到的真实缺陷（都留下了回归门）

1. **`"{MASTER_NAME} ...".format()` 会 KeyError**：占位符被当成 format 字段，冷落分支一走到就炸。
   由 ruff 的 `F524` 抓到，`test_injection.py` 留了 `test_gap_branches_are_all_reachable`。
2. **唯一分片兜底只在内存缓存里找**：面板早于第一次 tick 打开时会误报 `invalid_lanlan`。
   改为缓存空时回查一次持久化分片（`_single_known_lanlan`）。
3. **总开关关着时「索取陪伴」回 `company_cooldown`**：语义撒谎（不是冷却，是没开）。
   改为回 `not_enabled`。
4. **禁用期的冻结语义自相矛盾**：只把折算基准点推在内存里，进程重启后会一次性补算整段衰减，
   等于惩罚"关掉插件"的用户。改为启动时归一化一次并落盘；禁用期的每拍 tick 不写盘（避免写放大）。

### 测试基建（踩坑记录）

- pytest 9 的 importlib 模式会以模块名 `"__init__"` 导入仓根 `__init__.py`，
  触发"无父包的相对导入"崩溃；`tests/conftest.py` 预注册 `sys.modules["__init__"]`
  并强制目录按 `Dir` 收集。
- 挂载态（市场 CI 把仓库挂进宿主包树）下外层父包会被一起导入，轻量 venv 里没有那些依赖；
  预注册父包链的轻量桩挡住。
- 新增**隐私门**：会话记录里的 `content`（对话正文）在整个持久化状态里必须找不到。

**验证**：`129 → 130 passed`；`ruff`（CI 原样参数）全绿；五门全链通过。

### 已知但有意缓做

- 其余 6 语言 i18n 与注入模板多语言。
- 情绪感知（读对话判心情）——按需求留待后续以独立插件联动方式接入。
- LLM 工具在宿主重启后的自动重注册（宿主不自动重注册；本版靠重载插件恢复）。
